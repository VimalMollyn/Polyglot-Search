[project]
name = "multilingual-search"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "python-fasthtml>=0.12",
    "httpx>=0.27",
    "python-dotenv>=1.0",
]

[tool.hatch.build.targets.wheel]
include = ["app.py"]
